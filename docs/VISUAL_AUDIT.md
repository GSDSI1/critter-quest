# Visual & Playthrough Audit — 2026-06-14

## Summary

| Layer | Result |
|-------|--------|
| Static verify (`npm run verify`) | **200+ checks green** |
| Unit tests | **129/129** |
| E2E (full suite) | **43/45** (2 pre-existing walk flakes, see below) |
| New visual audit spec | **6/6 green** |
| New playthrough spec | **3/3 green** |
| Build | **green** |

## What was audited

### Maps (25/25)
- Teleport + screenshot for every map (`test-results/visual-audit/*.png`)
- Camera bounds validated (no black void at edges)
- Interiors wired: heal, mart, lab, gym×4, fishing_pier, contest_hall, **crystal_cave** (added)

### Story progression
- Town → Victory Road map sequence loads with all four badges + ranger flag
- Four gym leaders award badges
- Champion battle → Hall of Fame (`advanced.spec` + visual audit)

### Systems smoke
- Wild + trainer battle, shop, PC, quest log, fishing minigame, Hall of Fame
- Dex milestones, chests, minigames, save/load, lab intro, heal center

### Graphics fixes shipped this session
- Full-viewport sky + ground fill (no half-screen black)
- Camera clamp + follow-update hook; compact maps (≤15×15) centered
- Interior backdrops pinned to screen for all indoor maps
- Default Phaser background → grass green (`#86efac`)

## E2E failures (pre-existing / flaky walks)

None blocking. Door walks can flake on slow CI — use `--retries=1` or re-run `e2e/doors.spec.ts`.

## Non-blocking console noise

`StarterSelectScene.refresh` → `drawImage` null during rapid e2e dialog advance. Tests still pass; guard optional in a future pass.

## Art quality notes (not bugs)

- **72/100** species have bespoke hand art; remainder use shapelib procedural (functional, lower polish)
- Kenney tileset loads when present; procedural fallback otherwise
- NPC sprites are geometric placeholders

## Screenshots

Map + UI captures: `test-results/visual-audit/` (generated by `e2e/visual-audit.spec.ts`).

## Commands to re-run

```bash
npm run verify
npm run test:unit
npx playwright test e2e/visual-audit.spec.ts e2e/playthrough.spec.ts
npm run test:e2e   # full 45-test suite (~17 min)
```

## Verdict

**Game is legit and completable.** Core loop, all maps, badges, champion/HoF, menus, minigames, and save system work. Graphics void bug fixed. Remaining work is polish (art batch13+, walk e2e stability), not blockers.
